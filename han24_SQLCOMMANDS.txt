KEONI HAN 
PART 9 SQL COMMANDS:

Task 3
CREATING TABLE:
	CREATE TABLE ORDERS(ORDERID INTEGER, MONTH CHAR(3), DAY INTEGER, QUANTITY INTEGER, TOPPING VARCHAR(10), NOTES VARCHAR(512));

ADDING CONSTRAINT:
	ALTER TABLE ORDERS ADD  CHECK (TOPPING = 'plain' OR TOPPING = 'chocolate' OR TOPPING = 'cherry');

TASK 4:
	DESCRIBE ORDERS;

TASK 6:
	SELECT COUNT(*) FROM ORDERS;					61 orders (it isn't 62 rows because one row is a header row in the csv sheet)

Task 7:
	SELECT COUNT(*) FROM ORDERS WHERE MONTH = 'FEB';		18 orders

Task 8:
	INSERT INTO ORDERS VALUES(232, 'SEP', 13, 2, 'cherry', 'nothing');
